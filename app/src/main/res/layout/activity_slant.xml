<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin">
    <ImageView
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:scaleType="fitXY"
        android:src="@mipmap/small_icon"
        android:id="@+id/left_img" />
   <com.android.customview.view.SlantedTextView
       android:layout_width="50dp"
       android:layout_height="50dp"
       android:layout_alignParentTop="true"
       android:layout_alignParentStart="true"
       android:gravity="center"
       app:slantedBackgroundColor="@color/primary"
       app:slantedLength="28dp"
       app:slantedTextSize="10sp"
       app:slantedMode="left"
       app:slantedText="首发"
       app:slantedTextColor="@color/primary_light"/>
    <ImageView
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:layout_alignTop="@+id/left_img"
        android:scaleType="fitXY"
        android:src="@mipmap/small_icon"
        android:layout_alignParentEnd="true"
        android:id="@+id/right_img" />
    <com.android.customview.view.SlantedTextView
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:gravity="center"
        app:slantedBackgroundColor="@color/primary_dark"
        app:slantedLength="28dp"
        app:slantedTextSize="10sp"
        app:slantedMode="right_triangle"
        app:slantedText="JavaScript"
        app:slantedTextColor="@color/primary_light"
        android:layout_alignEnd="@+id/right_img"
        android:layout_alignTop="@+id/right_img"/>
</RelativeLayout>